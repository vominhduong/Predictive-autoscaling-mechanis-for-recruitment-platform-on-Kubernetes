import axios from 'axios';import type{ApiErrorBody}from'../types/api';
const messages:Record<string,string>={VALIDATION_ERROR:'Vui lòng kiểm tra lại thông tin.',INVALID_CREDENTIALS:'Email hoặc mật khẩu không đúng.',UNAUTHORIZED:'Phiên đăng nhập không hợp lệ.',FORBIDDEN:'Bạn không có quyền thực hiện thao tác này.',ACCESS_DENIED:'Bạn không có quyền thực hiện thao tác này.',RESOURCE_FORBIDDEN:'Bạn không có quyền truy cập tài nguyên này.',PROFILE_NOT_FOUND:'Chưa có hồ sơ ứng viên.',CV_NOT_FOUND:'Không tìm thấy CV.',JOB_NOT_FOUND:'Không tìm thấy việc làm.',JOB_APPLICATION_CLOSED:'Việc làm không còn nhận hồ sơ.',APPLICATION_ALREADY_EXISTS:'Bạn đã ứng tuyển công việc này.',DUPLICATE_APPLICATION:'Bạn đã ứng tuyển công việc này.',INVALID_STATUS_TRANSITION:'Không thể chuyển sang trạng thái đã chọn.',INVALID_APPLICATION_STATUS_TRANSITION:'Không thể chuyển sang trạng thái đã chọn.',OPTIMISTIC_LOCK_CONFLICT:'Dữ liệu vừa được cập nhật ở nơi khác. Hãy tải lại.',DEPENDENCY_UNAVAILABLE:'Dịch vụ tạm thời không khả dụng.',UPSTREAM_SERVICE_UNAVAILABLE:'Dịch vụ tạm thời không khả dụng.',UPSTREAM_SERVICE_TIMEOUT:'Dịch vụ phản hồi quá lâu.'};
export class AppError extends Error{constructor(public code:string,message:string,public fields:ApiErrorBody['fieldErrors']=[],public status?:number){super(message)}}
export function normalizeError(error:unknown){if(error instanceof AppError)return error;if(axios.isAxiosError<ApiErrorBody>(error)){const body=error.response?.data;const code=body?.code??'UNKNOWN_ERROR';return new AppError(code,messages[code]??body?.message??'Đã có lỗi xảy ra. Vui lòng thử lại.',body?.fieldErrors??[],error.response?.status)}return new AppError('UNKNOWN_ERROR','Đã có lỗi xảy ra. Vui lòng thử lại.')}
