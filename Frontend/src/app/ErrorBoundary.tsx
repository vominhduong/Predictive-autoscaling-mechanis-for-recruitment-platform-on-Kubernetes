import{Component,type ErrorInfo,type ReactNode}from'react';export class ErrorBoundary extends Component<{children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return{failed:true}}componentDidCatch(error:Error,info:ErrorInfo){if(import.meta.env.DEV)console.error('Application render failure',error.name,info.componentStack)}render(){return this.state.failed?<main className="state error"><h1>Ứng dụng gặp sự cố</h1><p>Vui lòng tải lại trang để tiếp tục.</p><button onClick={()=>location.reload()}>Tải lại</button></main>:this.props.children}}
