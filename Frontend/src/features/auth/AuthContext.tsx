import{createContext,useContext,useEffect,useSyncExternalStore,type PropsWithChildren}from'react';import{useNavigate}from'react-router-dom';import{getSession,setSession,subscribeSession}from'./session';import type{Session}from'../../types/api';
interface AuthValue{session:Session|null;save:(s:Session)=>void;clear:()=>void}const AuthContext=createContext<AuthValue|null>(null);
export function AuthProvider({children}:PropsWithChildren){const session=useSyncExternalStore(subscribeSession,getSession,getSession);const navigate=useNavigate();useEffect(()=>{const expired=()=>navigate('/login',{replace:true});window.addEventListener('auth:expired',expired);return()=>window.removeEventListener('auth:expired',expired)},[navigate]);return <AuthContext.Provider value={{session,save:s=>setSession(s),clear:()=>setSession(null)}}>{children}</AuthContext.Provider>}
export function useAuth(){const value=useContext(AuthContext);if(!value)throw new Error('AuthProvider missing');return value}
