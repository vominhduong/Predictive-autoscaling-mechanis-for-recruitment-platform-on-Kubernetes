import{z}from'zod';
export const uuid=z.string().uuid('UUID không hợp lệ');export const jobSchema=z.object({companyId:uuid,title:z.string().trim().min(1,'Nhập tiêu đề').max(255),description:z.string().trim().min(1,'Nhập mô tả').max(20000),requirements:z.string().trim().min(1,'Nhập yêu cầu').max(20000),locationId:uuid,categoryId:uuid,employmentType:z.enum(['FULL_TIME','PART_TIME','CONTRACT','INTERNSHIP','FREELANCE']),salaryMin:z.number().min(0).nullable(),salaryMax:z.number().min(0).nullable(),salaryCurrency:z.string().trim().regex(/^[A-Z]{3}$/,'Dùng mã tiền tệ 3 chữ cái'),salaryNegotiable:z.boolean(),applicationDeadline:z.string().refine(v=>new Date(`${v}T23:59:59`)>new Date(),'Hạn nộp phải ở tương lai'),version:z.number().nullable()}).refine(v=>v.salaryMin==null||v.salaryMax==null||v.salaryMax>=v.salaryMin,{message:'Lương tối đa phải lớn hơn hoặc bằng lương tối thiểu',path:['salaryMax']});
export type JobInput=z.infer<typeof jobSchema>;
