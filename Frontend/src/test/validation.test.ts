import{describe,expect,it}from'vitest';import{loginSchema,registerSchema}from'../features/auth/schemas';import{jobSchema}from'../features/jobs/schemas';import{applicationTransitions,validateCv}from'../utils/validation';
describe('client validation',()=>{it('rejects invalid login',()=>{expect(loginSchema.safeParse({email:'bad',password:''}).success).toBe(false)});it('registers only supported roles and strong passwords',()=>{expect(registerSchema.safeParse({email:'a@example.com',password:'Strong#123',role:'CANDIDATE'}).success).toBe(true);expect(registerSchema.safeParse({email:'a@example.com',password:'weak',role:'ADMIN'}).success).toBe(false)});it('checks salary and future deadline',()=>{const base={companyId:'22222222-2222-4222-8222-222222222222',title:'Engineer',description:'Description',requirements:'Requirements',locationId:'33333333-3333-4333-8333-333333333333',categoryId:'44444444-4444-4444-8444-444444444444',employmentType:'FULL_TIME',salaryMin:3000,salaryMax:2000,salaryCurrency:'USD',salaryNegotiable:false,applicationDeadline:'2000-01-01',version:null};expect(jobSchema.safeParse(base).success).toBe(false);expect(jobSchema.safeParse({...base,salaryMax:4000,applicationDeadline:'2099-01-01'}).success).toBe(true)});it('validates CV mime, extension and size',()=>{expect(validateCv(new File(['x'],'cv.txt',{type:'text/plain'}))).toMatch(/PDF/);expect(validateCv(new File([new Uint8Array(6*1024*1024)],'cv.pdf',{type:'application/pdf'}))).toMatch(/5 MB/);expect(validateCv(new File(['%PDF'],'cv.pdf',{type:'application/pdf'}))).toBeNull()});it('exposes only allowed application transitions',()=>{expect(applicationTransitions.APPLIED).toEqual(['SCREENING','REJECTED']);expect(applicationTransitions.REJECTED).toHaveLength(0)})});
