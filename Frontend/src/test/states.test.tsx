import{render,screen}from'@testing-library/react';import{describe,expect,it}from'vitest';import{Empty,ErrorState,Loading}from'../components/Ui';describe('shared states',()=>{it('renders loading',()=>{render(<Loading/>);expect(screen.getByRole('status')).toBeInTheDocument()});it('renders empty',()=>{render(<Empty/>);expect(screen.getByText('Chưa có dữ liệu')).toBeInTheDocument()});it('renders friendly error',()=>{render(<ErrorState message="Thử lại sau"/>);expect(screen.getByRole('alert')).toHaveTextContent('Thử lại sau')})});
