import{render,screen}from'@testing-library/react';import{MemoryRouter,Route,Routes}from'react-router-dom';import{describe,expect,it}from'vitest';import{normalizeError}from'../api/errors';import{Protected}from'../app/Guards';import{AuthProvider}from'../features/auth/AuthContext';import{createSession,setSession}from'../features/auth/session';import{jwt}from'./fixtures';
function view(role?:'CANDIDATE'|'EMPLOYER'){if(role)setSession(createSession({accessToken:jwt(role),refreshToken:'refresh',tokenType:'Bearer',expiresIn:100,refreshExpiresIn:1000}));return render(<MemoryRouter initialEntries={['/private']}><AuthProvider><Routes><Route path="/login" element={<p>Login</p>}/><Route path="/403" element={<p>Forbidden</p>}/><Route path="/private" element={<Protected role="CANDIDATE"><p>Private</p></Protected>}/></Routes></AuthProvider></MemoryRouter>)}
describe('security and error handling',()=>{it('redirects anonymous users to login',()=>{view();expect(screen.getByText('Login')).toBeInTheDocument()});it('allows matching role',()=>{view('CANDIDATE');expect(screen.getByText('Private')).toBeInTheDocument()});it('blocks mismatched role',()=>{view('EMPLOYER');expect(screen.getByText('Forbidden')).toBeInTheDocument()});it('maps backend errors without internal details',()=>{const error=normalizeError({});expect(error.message).toMatch(/lỗi/);expect(normalizeError(new Error('SQL select secret')).message).not.toMatch(/SQL/)})});
