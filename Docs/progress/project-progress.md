# Recruitment Platform — Project Progress

Last updated: 2026-09-23 (Asia/Bangkok)

## Completed

### Prompt 01 — Baseline infrastructure

- Local Docker Compose defines PostgreSQL, Redis, RabbitMQ, MinIO and Mailpit on `recruitment-network`.
- PostgreSQL initialization creates `auth_db`, `user_db`, `job_db`, `application_db` and `notification_db`.

### Prompt 02 — Auth foundation

- Auth Service uses Java 21 bytecode, Spring Boot, Security, JPA, Validation, Actuator, PostgreSQL and Flyway.
- Flyway V1 owns `users` and `refresh_tokens`; its existing migration was not changed.
- PostgreSQL integration tests validate schema, repository behavior and case-insensitive email uniqueness.

### Prompt 03 — Register

- `POST /api/v1/auth/register` supports Candidate and Employer registration.
- Email normalization, password policy, BCrypt storage, duplicate handling, response DTOs and standard API errors are implemented.

### Prompt 04 — Login, JWT, refresh and logout

- `POST /api/v1/auth/login` normalizes email, verifies BCrypt credentials and returns indistinguishable `INVALID_CREDENTIALS` failures.
- Locked and disabled accounts are rejected with their dedicated error codes.
- Access Tokens use RS256 with `sub = userId`, `email`, `role`, `iss`, `iat`, `exp` and `jti` claims.
- JWT signature, expiry and issuer are validated. Access Token lifetime defaults to 1800 seconds.
- Refresh Tokens are opaque URL-safe random values; only SHA-256 hashes are stored in PostgreSQL.
- Refresh rotation uses a pessimistic database lock and revokes the previous token transactionally.
- Concurrent reuse permits exactly one successful rotation.
- `POST /api/v1/auth/logout` requires an Access Token, revokes the supplied Refresh Token idempotently and returns 204.
- Spring Security 401/403 responses use the standard JSON error contract with correlation/trace ID.
- Token lifecycle decisions use an injectable UTC `Clock` where deterministic time matters.
- Architecture docs now consistently reserve Redis for later cache/rate-limit/blacklist work and document PostgreSQL Refresh Token hashes.

### Prompt 05 — API Gateway

- API Gateway uses Spring Cloud Gateway Server WebFlux; it does not include Spring MVC, JPA, PostgreSQL, Flyway or Eureka.
- Public routes forward Auth, User, Job and Application API prefixes to configurable upstream URIs. Internal endpoints are not exposed.
- The Gateway verifies RS256 Access Tokens with the public key and issuer, reads the user ID from `sub`, and never contains a private signing key.
- Client-supplied `X-User-Id`, `X-User-Email` and `X-User-Role` headers are removed before verified identity headers are added.
- Security failures and upstream failures use the standard JSON error contract. Correlation IDs are validated, forwarded and returned.
- CORS is configurable by exact allowed origins. Connect and response timeouts are configured globally.

### Prompt 06 — User Service, Company and CV

- User Service runs on port 8082 and owns only `user_db`; Hibernate uses `ddl-auto=validate` and Flyway owns schema changes.
- Flyway V1 creates `candidate_profiles`, `companies`, `company_members` and `cvs` with UUID keys, uniqueness constraints, indexes, timestamps and optimistic-lock versions for mutable aggregate roots.
- Candidate endpoints create/read/update the authenticated user's profile and upload/list/delete that user's PDF CV metadata.
- CV validation checks extension, MIME type, PDF signature and configured size. Object keys are generated from trusted UUIDs and are not exposed in public responses.
- Company creation atomically creates its creator as OWNER. OWNER can update the Company and add RECRUITER members; duplicate members and client-assigned OWNER roles are rejected.
- Internal CV ownership and Company authorization contracts require a separate internal token and are not routed by Gateway.
- MinIO stores file bytes in the private `recruitment-cvs` bucket; PostgreSQL stores metadata and the private object key.
- OpenAPI JSON and Swagger UI are generated by springdoc-openapi 3.x for Spring Boot 4.

## Verification

Environment:

- Docker Desktop server 29.6.2.
- PostgreSQL Testcontainers image `postgres:17-alpine`.
- IntelliJ bundled JBR ran Maven with Java 25.0.3 while Maven compiled with `release 21`.

Commands:

```powershell
$env:JAVA_HOME = 'C:\Program Files\JetBrains\IntelliJ IDEA 2026.2\jbr'
.\mvnw.cmd clean verify
& 'C:\Program Files\JetBrains\IntelliJ IDEA 2026.2\jbr\bin\java.exe' -jar target\auth-service-0.0.1-SNAPSHOT.jar
```

Automated result on 2026-09-22:

- `BUILD SUCCESS`
- Tests run: 81
- Failures: 0
- Errors: 0
- Skipped: 0
- Includes Flyway/PostgreSQL integration, register/login, JWT signature/expiry/issuer, hashed token persistence, rotation/replay, expired/revoked/random token, concurrent refresh, authenticated logout and standard security errors.

Manual result against local `auth_db` and Auth Service port 8081:

- Register: success (`201` semantic response).
- Login: Bearer token pair, Access Token TTL 1800 seconds, Refresh Token TTL 604800 seconds.
- Refresh: returned a different Refresh Token.
- Reuse old Refresh Token: `401`.
- Authenticated logout: `204`.
- Refresh after logout: `401`.
- Database check: stored token hashes are 64 lowercase hexadecimal characters; raw tokens were not printed or persisted.

The first verification attempt could not initialize Testcontainers because Docker Desktop was stopped. Docker Desktop was started and the full command was rerun successfully. The Auth Service process used for manual verification was stopped cleanly afterward.

### API Gateway verification

Environment and resolved compatibility:

- Spring Boot 4.1.1.
- Spring Cloud BOM 2025.1.3.
- Spring Cloud Gateway Server WebFlux 5.0.3.
- Maven ran with the IntelliJ bundled JBR and compiled with Java release 21.

Automated result on 2026-09-22:

- Baseline before Prompt 05 changes: 1 test passed.
- Final `mvnw.cmd clean verify`: 13 tests passed, 0 failures, 0 errors and 0 skipped.
- Coverage includes routing and body preservation, public-route identity stripping, valid JWT identity propagation, missing/tampered/expired JWT rejection, internal-route denial, correlation ID behavior, CORS, upstream unavailable and upstream timeout.

Manual result with Auth Service on port 8081 and Gateway on port 8080:

- Register through Gateway: `201`.
- Login through Gateway: `200` and a valid RS256 Access Token.
- Missing token: `401`.
- Valid JWT passed Gateway authentication and reached an unavailable protected Application Service upstream: `503`.
- Tampered-signature token: `401`.
- Internal Auth endpoint through Gateway: `404`.
- Correlation ID `manual-gateway-001` was returned unchanged.
- Allowed-origin CORS preflight: `200`, with the configured origin and credentials response headers.
- Expired-token rejection, verified-header replacement and timeout mapping were additionally exercised by automated integration tests with controlled upstreams.
- Auth Service and Gateway processes used for manual verification were stopped cleanly afterward.

### User Service verification

- Baseline before Prompt 06 failed its only context test because no datasource driver/configuration could be resolved.
- Final `mvnw.cmd clean verify`: 16 tests passed, 0 failures, 0 errors and 0 skipped.
- PostgreSQL 17 Testcontainers verified Flyway V1 and repositories without using the development database.
- Tests cover profile ownership and role checks, Company OWNER transaction/policy, outsider and recruiter denial, duplicate membership, public response filtering, PDF extension/MIME/signature/size validation, safe object keys, cross-user CV deletion denial, internal contracts and upload cleanup after database failure.
- Manual Gateway flow passed Candidate register/login/profile update/CV upload/list/delete and Employer register/login/Company create/update.
- Manual unauthorized Company update returned 403, and `/internal/**` through Gateway returned 404.
- MinIO contained the `recruitment-cvs` bucket with private access; the manual CV metadata and object were removed after DELETE.
- Auth Service, User Service and Gateway processes used for manual verification were stopped cleanly afterward.

### Job Service completed

- Flyway V1 creates `jobs`, `job_categories` and `job_locations` in `job_db`, including enum/salary constraints, indexes and optimistic versioning; Hibernate uses `ddl-auto=validate`.
- Job domain, Category/Location validation, DRAFT creation, update, status transitions, public search/filter/pagination and employer Company listing are implemented.
- Job mutations require a validated EMPLOYER Gateway identity and call User Service's internal Company authorization contract for OWNER/RECRUITER membership before opening the database transaction.
- Internal `GET /internal/jobs/{jobId}/eligibility` requires `X-Internal-Token` and returns the minimal Application Service snapshot. Gateway does not route internal paths.
- Standard success/error envelopes, correlation IDs, upstream timeout/unavailable mapping, Actuator and OpenAPI are implemented.

### Job Service verification

- Runtime: IntelliJ IDEA 2026.2 bundled JBR, Java 25.0.3; Maven compiler release 21; Spring Boot 4.1.1.
- Job Service `mvnw.cmd clean verify`: 12 tests passed, 0 failures, 0 errors, 0 skipped.
- PostgreSQL Testcontainers image: `postgres:17-alpine` (PostgreSQL 17.11 during the final run). Tests verify Flyway on a fresh database, Hibernate validation, persistence, combined search filters, public visibility, role/Company authorization, reference/salary/deadline validation, transitions/version conflict, internal authentication, correlation ID and OpenAPI.
- Gateway regression `mvnw.cmd clean verify`: 13 tests passed, 0 failures, 0 errors, 0 skipped.
- Manual Gateway flow passed Employer registration/login, Company creation, DRAFT creation, publish, public search/detail, update, hide, republish and outsider rejection (403). Public detail returned 404 for DRAFT/HIDDEN Jobs.
- Authenticated `/internal/**` through Gateway returned 404. Direct eligibility returned `acceptingApplications=true` with the valid service token and 401 with an invalid token.
- PostgreSQL inspection confirmed the final `PUBLISHED` Job in `job_db`. Auth, User, Job and Gateway processes plus PostgreSQL/MinIO started for verification were stopped afterward.

## In progress

- None for Prompt 08.

## Blocked

- None.

## Remaining security/operational limitations

- Development RSA keys are local-only examples. Deployments must inject their own key paths and must not use the development private key.
- Refresh Token storage remains PostgreSQL-backed for the MVP. Redis integration, rate limiting and token blacklist behavior belong to later prompts.
- Key rotation/JWKS and production secret management are not part of Prompt 04.
- Rate limiting, retries, service discovery and circuit breaking are outside Prompt 05.
- Internal service authentication uses an environment-provided shared token plus private networking for the MVP. Production should use workload identity or mutual TLS with rotation.
- CV upload compensates a failed database write by best-effort object deletion. Delete removes the object before metadata; an idempotent retry completes cleanup if the database step fails.
- Job search uses PostgreSQL/JPA rather than Elasticsearch. Cross-service Company authorization has an unavoidable TOCTOU boundary, and the shared internal token is an MVP/private-network mechanism.
- Admin Job moderation and asynchronous Job events are outside Prompt 07.

### Application Service completed

- Flyway V1 creates `applications` and `application_status_histories` in `application_db`, with logical cross-service IDs, immutable snapshots, unique `(candidate_id, job_id)`, status constraints, indexes and optimistic versioning.
- Candidate apply validates Job eligibility and CV ownership through internal Job/User APIs before opening the database transaction. The database unique constraint remains the concurrency authority.
- Candidate list/detail and Employer Job list/detail enforce role and ownership. Employer authorization uses Job's Company snapshot plus User Service Company authorization, preserving OWNER/RECRUITER behavior.
- State transitions implement APPLIED to SCREENING/REJECTED, SCREENING to INTERVIEW/REJECTED, INTERVIEW to OFFER/REJECTED and OFFER to HIRED/REJECTED. Every accepted transition and initial submission creates history.
- Internal GET calls use bounded connect/read timeouts, one retry for idempotent requests, separate Job/User circuit breakers and standard 503 timeout/unavailable errors.
- Standard envelopes, correlation IDs, pagination/sort whitelist, Actuator and OpenAPI are implemented. Existing Gateway Application routes required no configuration change.

### Application Service verification

- Runtime: IntelliJ IDEA 2026.2 bundled JBR, Java 25.0.3; Maven compiler release 21; Spring Boot 4.1.1.
- Application Service `mvnw.cmd clean verify`: 10 tests passed, 0 failures, 0 errors, 0 skipped.
- PostgreSQL Testcontainers image `postgres:17-alpine` ran PostgreSQL 17.11. Coverage includes fresh Flyway/Hibernate validation, snapshots, role/ownership, invalid Job/CV, sequential and concurrent duplicate apply, private queries, complete transition/history flow, terminal states, optimistic conflict, dependency timeout/no partial write, correlation ID and OpenAPI.
- Gateway regression `mvnw.cmd clean verify`: 13 tests passed, 0 failures, 0 errors, 0 skipped.
- Manual E2E through Gateway passed Employer/Company/Job publish, Candidate profile/CV upload/apply, duplicate 409, Candidate list/detail, Employer list, outsider 403 and the full transition chain to HIRED. A terminal transition returned 409.
- Direct `application_db` inspection confirmed one Application, immutable Job/Candidate identity snapshots and five history rows. All Java processes and PostgreSQL/MinIO started for verification were stopped afterward.
- The current User internal CV contract does not return Candidate name. Application Service records the verified Gateway email as `candidate_identity_snapshot` without expanding User Service scope.

### Prompt 09 — RabbitMQ and Notification Service completed

- Application Service writes versioned `APPLICATION_SUBMITTED` and `APPLICATION_STATUS_CHANGED` JSON events to `outbox_events` in the same transaction as the Application mutation. A scheduled publisher locks and claims rows, sends persistent messages, waits for correlated RabbitMQ confirmation, and only then marks rows `PUBLISHED`; bounded backoff retains failures for retry and audit.
- Notification Service runs on port 8085, owns only `notification_db`, applies Flyway V1, and starts Hibernate with `ddl-auto=validate`.
- RabbitMQ declares durable topic exchange `recruitment.events`, durable queue `notification.application-events`, routing keys `application.submitted` and `application.status-changed`, durable DLX `notification.events.dlx`, and durable DLQ `notification.application-events.dlq`.
- The consumer validates the independent versioned contract, uses bounded retry and dead-letter recovery, persists `notifications` plus `processed_events`, and suppresses redelivery using unique `event_id` values.
- Spring Mail sends plain-text submission and status-change templates to local Mailpit. Metrics cover consumed events, successful/failed email, duplicates, retries, and DLQ recovery.
- No public Notification API was added because the current contract does not require one. No Notification or internal route was exposed through Gateway.

### Prompt 09 verification

- Runtime: OpenJDK 26.0.1; Maven compiler release 21; Spring Boot 4.1.1; Docker Desktop 29.6.2.
- Application Service `mvnw.cmd clean verify`: 14 tests passed, 0 failures, 0 errors, 0 skipped. PostgreSQL Testcontainers image: `postgres:17-alpine`.
- Notification Service `mvnw.cmd clean verify`: 6 tests passed, 0 failures, 0 errors, 0 skipped. Testcontainers images: `postgres:17-alpine` and `rabbitmq:4-management-alpine`.
- API Gateway regression `mvnw.cmd clean verify`: 13 tests passed, 0 failures, 0 errors, 0 skipped.
- Manual Gateway flow passed Employer and Candidate registration/login, Company and published Job creation, PDF CV upload, Application submission, and Employer transition to `SCREENING`.
- Direct inspection found one tested Application and two `PUBLISHED` outbox rows in `application_db`; `notification_db` contained exactly two distinct `SENT` notifications for that flow.
- Mailpit received the submission and status-change emails. Re-publishing the same event ID changed neither the Mailpit count nor the notification row count.
- Stopping Mailpit exercised three bounded SMTP attempts and produced one DLQ message. RabbitMQ management confirmed the durable exchange, queue, both bindings, and durable DLQ.
- Gateway kept internal endpoints unavailable publicly. All Java processes and infrastructure containers started for verification were stopped afterward.

### Prompt 09 operational limitations

- Delivery is at least once: a broker ACK can be lost after RabbitMQ accepted a message, so the outbox may publish it again. Consumer idempotency handles normal redelivery.
- SMTP and the Notification database transaction cannot be atomic with Mailpit. A crash after SMTP accepts mail but before the database commit can still produce a duplicate on redelivery because Mailpit has no idempotency-key contract.
- Published outbox rows remain for audit. Automated retention or archival is an operational follow-up outside Prompt 09.

### Prompt 10 — Frontend completed

- Added the React 19, TypeScript, Vite, React Router, TanStack Query, React Hook Form, Zod and Axios frontend in `Frontend/`, with feature-oriented routes, shared API/error handling, responsive styles and application-level error handling.
- Authentication supports Candidate/Employer registration, login, backend logout, session restoration and single-flight access-token refresh. Protected and role routes use the session role for navigation while backend authorization remains authoritative; unsafe redirect targets are rejected.
- Public pages provide the home page, server-side Job search, URL-backed filters/sort/pagination and Job detail. Job filters follow the backend `EmploymentType` and sort whitelists.
- Candidate pages provide owned profile create/update, PDF CV validation/upload/list/delete, Job application with duplicate-submit protection, and paginated Application list/detail/history.
- Employer pages provide Company create/update and Recruiter membership, Job create/update/status transitions, and Application listing/detail/status transitions. Because the backend has no Employer Company-list endpoint, successfully created Companies are remembered in browser storage for navigation; server authorization still governs every request.
- Notification UI states that notifications are delivered by email because Prompt 09 intentionally exposes no public Notification API. No fake notification data or backend endpoint was introduced.
- The browser calls only the API Gateway base URL configured by `VITE_API_BASE_URL`; it never creates trusted `X-User-*` headers or exposes an internal service token.

### Prompt 10 verification

- Runtime: Node.js 24.13.0 and npm 11.6.2.
- Clean dependency verification: `npm ci` succeeded after synchronizing `package-lock.json` with `package.json`.
- `npm run lint`: passed with zero warnings; `npm run typecheck`: passed with zero TypeScript errors.
- `npm run test`: 24 tests passed across 6 files, 0 failed and 0 skipped. Coverage includes validation, route guards, error states/mapping, API contracts and single-flight refresh/logout behavior.
- `npm run build`: production build passed. The main minified JavaScript chunk is 572.20 kB (181.85 kB gzip), which produces Vite's advisory chunk-size warning.
- Mocked Playwright verification: 5 tests passed and 3 environment/project-specific tests skipped across desktop Chromium and mobile emulation.
- Live Playwright verification through API Gateway: 1 critical end-to-end test passed. It verified Employer registration/login, Company and Job creation, publish/hide/republish/close transitions, public Job search/detail, Candidate registration/login/profile/CV upload/apply/duplicate prevention/Application detail, Employer status update, Candidate status refresh, CV deletion, cross-role route denial, cross-Employer ownership denial, logout and absence of browser-supplied trusted identity headers.
- Responsive navigation and horizontal overflow were checked in mobile emulation. The Vite process used by Playwright was stopped by its web-server lifecycle.

### Prompt 10 remaining limitations

- The backend currently has no Employer Company-list API and no public Category/Location discovery API. Company navigation is browser-local after successful creation, and Job forms require administrator-provided Category/Location UUIDs.
- Notification Service exposes email delivery only, so there is no unread count, notification list or mark-as-read UI.
- Advanced SSR/SEO, social login, analytics, full WCAG audit and route-level bundle splitting are not included. The current production bundle emits a non-failing chunk-size advisory.
- `npm audit` reports two moderate transitive dependency findings; no forced major-version remediation was applied during Prompt 10.
- Containerization and observability remain Prompt 11 work.

### Prompt 11 — Docker Compose and Observability completed

- Added multi-stage Maven/JRE 21 Dockerfiles for Auth, Gateway, User, Job, Application and Notification services. Runtime images use an unprivileged `app` user, a read-only filesystem with `/tmp` tmpfs, dropped capabilities, `no-new-privileges`, graceful SIGTERM handling and Actuator readiness health checks.
- The Auth image is assembled as an exploded Spring Boot application after removing the development private key. Compose mounts the local-only signing key read-only at runtime; Gateway contains only the public verification key.
- Added a Node 24 multi-stage Frontend build and unprivileged Nginx runtime with React Router fallback, immutable hashed-asset caching, no-cache `index.html`, same-origin `/api` reverse proxy, health endpoint and baseline browser security headers.
- Root `compose.yml` includes the production-like local topology in `infrastructure/docker-compose.yml`: Frontend, six Spring services, PostgreSQL, RabbitMQ, MinIO with deterministic private-bucket initialization, Mailpit, reserved Redis, Prometheus and Grafana.
- Domain services use Docker DNS and expose no host ports. Frontend and Gateway are the public application entry points; local infrastructure/observability UIs retain documented diagnostic ports.
- PostgreSQL uses a named volume and idempotent fresh-volume database initialization for `auth_db`, `user_db`, `job_db`, `application_db` and `notification_db`; Flyway remains the schema owner and Hibernate remains `ddl-auto=validate`.
- Added Micrometer Prometheus registry to all Spring services and exposed only health, info and Prometheus Actuator endpoints. Logs remain on stdout/stderr with service and correlation-ID fields; Loki was omitted to avoid Docker socket or broad host-log mounts.
- Prometheus scrapes all six services directly on the internal network. Grafana provisions the Prometheus datasource and a seven-panel Recruitment Platform dashboard for request rate, error rate, p95 latency, JVM heap, CPU, Hikari usage and application messaging metrics.
- Added root README instructions for configuration, build/start/stop, local URLs, retained-volume behavior, destructive reset warning and troubleshooting.

### Prompt 11 verification

- Docker Desktop Engine 29.6.2 and Docker Compose v5.3.1.
- `docker compose config --quiet` passed. `docker compose build --no-cache` built all seven application images successfully.
- Backend regression: Auth 81, Gateway 13, User 16, Job 12, Application 14 and Notification 6 tests passed; total 142 passed, 0 failures/errors/skips.
- Frontend clean verification: `npm ci`, lint, typecheck and production build passed; 24 tests passed across 6 files. The existing advisory remains for the 572.92 kB minified main chunk.
- All required long-running containers reached `healthy`; MinIO init exited successfully after creating/confirming the private bucket. Application Service restart returned to healthy and the Application row count remained 7 before and after restart.
- Production-image Playwright E2E through `http://localhost:3000` passed. It exercised Candidate and Employer registration/login, Company/Job creation and transitions, profile/CV/application flows, duplicate prevention, ownership/role denial, status notification and logout through Nginx → Gateway.
- Prometheus reported 6/6 targets up and returned HTTP request metrics plus Notification custom counters. Grafana API confirmed the provisioned Prometheus datasource and seven-panel `Recruitment Platform Overview` dashboard.
- RabbitMQ contained the durable notification queue and DLQ. Mailpit contained three messages from the verified flows: one from an earlier interrupted run and the submission/status pair from the passing run; the duplicate Application attempt did not add an email.
- MinIO reported the `recruitment-cvs` bucket as private. Auth image inspection confirmed the private key was absent; Frontend contained no source maps, internal token or trusted identity-header marker.
- Authenticated access to `/internal/**` through Gateway returned 404. Container inspection confirmed `app` for all Spring services and UID 101 for Frontend. Log checks found neither the internal token nor an AMQP user/password credential string.

### Prompt 11 remaining limitations

- This Compose topology is production-like local infrastructure, not a production deployment. It has no TLS termination, secret manager, HA database/broker/object storage, autoscaling or disaster-recovery automation.
- Local credentials and the local-only RSA key are supplied through `.env`/read-only bind mount. A deployment must use generated credentials, externally managed signing keys and a secret manager.
- Redis is healthy and persistent but remains reserved for future cache/rate-limit/token-blacklist work.
- Logs are structured enough for stdout collection and correlation-ID inspection, but centralized aggregation and distributed tracing are not installed because safe local collection without Docker socket or broad host mounts was preferred.
- Resource limits require workload-specific tuning. Frontend route-level bundle splitting remains a performance follow-up.
- Kubernetes and release automation are outside Prompt 11.

## Next task

- Prompt 12 — CI/CD, Kubernetes and Release, only when explicitly requested.
